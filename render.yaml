services:
  - type: web
    name: tic-tac-toe-multiplayer
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
